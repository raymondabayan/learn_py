"""
Functions are operations that can be called or also known sometimes as methods when they belong to a class.
A function allows you to repeat the same logic, which provides consistency and reproducibility in your code.
"""
# Structure
def add_two(x, y):
    z = x + y
    return z

add_two(1, 4)

def square(x, y):
    if int(x) && int(y):
        r = x**y
    else
                
